<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.BugBustersStaff"
        tools:targetApi="31">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MenuInicial" />
        <activity android:name=".activities.PedidosActivosActivity" />
        <activity android:name=".activities.GenerarQRActivity" />
        <activity android:name=".activities.GenerarCuentaActivity" />
        <activity android:name=".activities.DetallePedidoActivity" />
        <activity android:name=".activities.FacturasPendientesActivity" />
    </application>

</manifest>